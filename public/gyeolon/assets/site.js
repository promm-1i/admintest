document.documentElement.classList.add('js');
const menuBtn=document.querySelector('.menu-btn'),mobileMenu=document.querySelector('.mobile-menu');
menuBtn?.addEventListener('click',()=>{const open=mobileMenu.classList.toggle('open');document.body.classList.toggle('menu-open',open);menuBtn.setAttribute('aria-expanded',String(open))});
document.querySelector('.to-top')?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));
const heroImgs=[...document.querySelectorAll('.hero-slides img')];let heroIndex=0;if(heroImgs.length>1&&!matchMedia('(prefers-reduced-motion:reduce)').matches){setInterval(()=>{heroImgs[heroIndex].style.opacity=0;heroIndex=(heroIndex+1)%heroImgs.length;heroImgs[heroIndex].style.opacity=1;},6000)}
const facImgs=[...document.querySelectorAll('.facility-stage img')],facTabs=[...document.querySelectorAll('[data-facility]')],facNum=document.querySelector('.facility-controls b');let fac=0;function showFac(n){if(!facImgs.length)return;fac=(n+facImgs.length)%facImgs.length;facImgs.forEach((el,i)=>el.classList.toggle('active',i===fac));facTabs.forEach((el,i)=>el.classList.toggle('active',i===fac));if(facNum)facNum.textContent=String(fac+1).padStart(2,'0')}facTabs.forEach((b,i)=>b.addEventListener('click',()=>showFac(i)));document.querySelector('[data-fac-prev]')?.addEventListener('click',()=>showFac(fac-1));document.querySelector('[data-fac-next]')?.addEventListener('click',()=>showFac(fac+1));showFac(0);
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('seen')}),{threshold:.12});document.querySelectorAll('section').forEach(s=>observer.observe(s));
